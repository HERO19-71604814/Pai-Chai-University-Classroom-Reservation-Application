<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic"
        android:label="배재대 강의실 예약"
        android:roundIcon="@drawable/ic"
        android:supportsRtl="true"
        android:theme="@style/Theme.PaiChaiUniversityClassroomReservationApplication"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".SignUpActivity"/>
        <activity android:name=".PasswordResetActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".DaytimeActivity"/>
        <activity android:name=".TableActivity"/>
        <activity android:name=".ConfirmActivity"/>
        <activity android:name=".EndActivity"/>
        <activity android:name=".MemberInitActivity" />
    </application>

</manifest>